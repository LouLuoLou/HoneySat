services:
  novnc:
    image: vncimage
    container_name: vnc
    build: .
    environment:
      ROOT_PASSWORD: kEmju7vu
      SCR_WIDTH: 1920
      SCR_HEIGHT: 1200
      VNC_RESIZE: scale
      HOME: /root
    ports:
      - "8080:8080"
      - "5900:5900"

# sudo docker build -t vncimage .
